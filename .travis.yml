dist: xenial
cache:
  directories:
    - download
language: python
env:
  - VERSION=2.8.5 MD5=8e48b6015464ed199d26080b2ff02d65
python:
  # https://docs.python.org/devguide/#status-of-python-branches
  - "2.7"
  - "3.6"
  - "3.7"
  - "pypy2.7-6.0"
  - "pypy3.5-6.0"
before_install:
  - ./install-hdfs.sh
  - pip install codecov
install:
  - pip install -e .
  - pip install --upgrade -r dev_requirements.txt
script: PATH=hadoop/bin:$PATH PYTHONHASHSEED=random pytest
after_success: codecov
