dist: xenial
cache:
  directories:
    - download
language: python
env:
  - VERSION=2.7.7 MD5=cc2f019f2a41458df8430c448bb9f760
python:
  # https://docs.python.org/devguide/#status-of-python-branches
  - "2.7"
  - "3.6"
  - "3.7"
  - "pypy2.7-6.0"
  - "pypy3.5-6.0"
before_install:
  - ./install-hdfs.sh
  - pip install codecov
install:
  - pip install -e .
  - pip install --upgrade -r dev_requirements.txt
script: PATH=hadoop/bin:$PATH PYTHONHASHSEED=random pytest
after_success: codecov
